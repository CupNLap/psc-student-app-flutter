import 'package:flutter/material.dart';

class AppTheme {
  // COLOR
  static const Color background = Color(0xFFF00503);
  static const Color background2 = Color(0xFF242424);
  
  static const Color accentColor = Color(0xFF4C0201);
  
  static const Color textTitle = Color.fromARGB(255, 222, 222, 222);
  static const Color textBody = Color.fromARGB(255, 255, 255, 255);
  
  static const Color shadow = Color.fromARGB(149, 0, 0, 0);
  static const Color divider = Color(0xFFCACACA);

  // MARGIN
  static const double smallMargin = 8.0;
  static const double mediumMargin = 16.0;
  static const double largeMargin = 24.0;

  // PADDING
  static const double smallPadding = 8.0;
  static const double mediumPadding = 16.0;
  static const double largePadding = 24.0;

  // GUTTER
  static const double smallGutter = 8.0;
  static const double mediumGutter = 16.0;
  static const double largeGutter = 24.0;

  // RADIUS
  static const double smallRadius = 10.0;
  static const double mediumRadius = 20.0;
  static const double largeRadius = 40.0;

  // TEXT
  static const double smallText = 12.0;
  static const double mediumText = 16.0;
  static const double largeText = 24.0;

  
}
